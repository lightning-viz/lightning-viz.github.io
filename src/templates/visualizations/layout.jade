extends ../layout

mixin editorJS(name, moduleName)
    script(src="http://public.lightning-viz.org/js/dynamic/viz/?visualizations[]=#{name}")
    script.
        window.lightning = window.lightning || {};
        window.lightning.editor = {
            moduleName: '#{moduleName}'
        };

mixin heading(name)
    h2
        | #{name}

block content

    .top.bar

    .documentation-container
        .row
            .column-3.hidden-mobile.overview-container
                - var section = 'plots'
                include ../includes/sidebar

            //- .column-1
            .column-3.hidden-mobile.empty-container
            .column-7.mobile-column-3.details-container

                section(name="introduction")
                    .row
                        .column-12
                            block heading
                            block container
                                #editor-component
                

    .bottom.bar.visible-mobile


block scripts
    script(src='//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js')
    block editor-scripts
    script(src='#{STATIC_URL}js/editor.js')
