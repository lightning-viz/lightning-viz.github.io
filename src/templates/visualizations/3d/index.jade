extends ../../layout


block content

    .top.bar


    .documentation-container
        .row
            .column-3.hidden-mobile.overview-container

                include ../../includes/visualizations/sidebar


            //- .column-1
            .column-3.hidden-mobile.empty-container
            .column-7.mobile-column-3.details-container
                header            
                    .row
                        .column-12
                            .row.header
                                .column-1.mobile-column-1
                                    img(src="/images/logo.png")

                                .column-10.mobile-column-3
                                    h1
                                        | Lightning

                                    | DATA VISUALIZATION SERVER


                section(name="introduction")
                    .row
                        .column-12
                                a(name="visualizations", href="#visualizations")
                                    h2
                                        | Visualizations
                



                section(name="3d")
                    .row
                        .column-12
                                a(name="3d", href="#3d")
                                    h2
                                        | 3D



                                a(name="scatter3", href="#scatter3")
                                    h4
                                        | Scatter3

                                p
                                    | A scatter3 visualization produces a three dimensional scatter plot using three.js. The camera can be controled by using the WASD and arrow keys. Use WASD to move the 
                                    | camera's position, and use the arrow keys to change the direction of the focus (FPS style controls).



                                #particles-iframe(data-url="http://lightning-docs.herokuapp.com/visualizations/146/pym", data-pym)
                                
                                .tab-container
                                    ul
                                        li
                                            a(href="#particles-data-tab")
                                                | Data Structure
                                        li
                                            a(href="#particles-python-tab")
                                                | Python Example

                                    .content-container

                                        #particles-data-tab
                                            pre
                                                code.
                                                    data = {
                                                        "points": [[x0,y0,z0], [x1,y1,z0], ... ]
                                                        "color": [[r0,g0,b0], [r1,g1,b1], ...]
                                                        "group": [g0, g1, ...]
                                                    }

                                        #particles-python-tab

                                            pre
                                                code.
                                                    from numpy import random, column_stack

                                                    n = 500
                                                    x = random.rand(n)*255
                                                    y = random.rand(n)*255
                                                    z = random.rand(n)*255
                                                    c = column_stack((x, y, z))

                                                    lgn.scatter3(x, y, z, color=c, size=3)

                                    
                                a(name="volume", href="#volume")
                                    h4
                                        | Volume

                                p
                                    | Render a collection of images as a three dimensional volume. The images are overlayed on eachother transparently to create a three dimensional effect. This uses the 
                                    | same FPS-style controls as the particles visualization.
                                

                                #volume-iframe(data-url="http://lightning-docs.herokuapp.com/visualizations/169/pym", data-pym)
                                
                                .tab-container
                                    ul

                                        li
                                            a(href="#volume-python-tab")
                                                | Python Example

                                    .content-container


                                        #volume-python-tab

                                            pre
                                                code.
                                                    from numpy import ndarray, linspace
                                                    
                                                    nx, ny, nz = (50, 50, 50)
                                                    rgb = ndarray((nx,ny,nz,3))
                                                    hsv = ndarray((nx,ny,nz,3))
                                                    for i, ii in enumerate(linspace(0,1,nx)):
                                                        for j, jj in enumerate(linspace(0,1,ny)):
                                                            for k, kk in enumerate(linspace(0,1,nz)):
                                                                position = (i, j, k)
                                                                rgb[position] = (kk, jj, ii)

                                                    lgn.volume([x for x in rgb])

                section(name="plots")
                    .row
                        .column-12
                                a(name="plots", href="/visualizations")
                                    h2
                                        | Plots
                                
                                a(href="/visualizations")
                                    h4
                                        | Click to view plot-based visualizations

                section(name="images")
                    .row
                        .column-12
                                a(name="images", href="/visualizations/images")
                                    h2
                                        | Images

                                a(href="/visualizations/images")
                                    h4
                                        | Click to view image-based visualizations


                section(name="streaming")
                    .row
                        .column-12
                                a(name="streaming", href="/visualizations/streaming")
                                    h2
                                        | Streaming

                                a(href="/visualizations/streaming")
                                    h4
                                        | Click to view streaming visualizations

                                

    .bottom.bar.visible-mobile